Certificate-Based Authentication System

This project implements a certificate-based authentication system using a combination of React, Spring Boot, and Electron. The system verifies user identity by reading a certificate from a USB smart card and matching it with login credentials.
